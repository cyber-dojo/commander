
version: '2.2'

services:
  nginx:
    env_file:
      - ${CYBER_DOJO_ENV_ROOT}/nginx.env
  web:
    env_file:
      - ${CYBER_DOJO_ENV_ROOT}/web.env
  grafana:
    env_file:
      - ${CYBER_DOJO_ENV_ROOT}/grafana.env
